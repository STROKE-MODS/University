#include <stdio.h>

int main(){
    int variable1,variable2;
    //Defining the variable
    printf("Enter the first number : ");
    scanf("%d",&variable1); //Initializing first variable
    printf("Enter the second number : ");
    scanf("%d",&variable2);//Initializing second variable

    printf("The first number is : %d\n The second number is  : %d",variable2,variable1);
    //Swapping the places of first and second variable
    
    return 0;
}